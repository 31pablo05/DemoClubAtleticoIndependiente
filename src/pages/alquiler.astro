---
import Layout from '../components/layout/Layout.astro';
import Hero from '../components/ui/Hero.astro';
---

<Layout 
  title="Alquiler de Canchas - Club Atlético Independiente de Trelew"
  description="Alquilá nuestras canchas para partidos particulares, eventos y torneos. Instalaciones de primera calidad."
>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@400;600;700&family=Barlow:wght@300;400;500&display=swap');

  :root {
    --red:    #D42B2B;
    --red-2:  #B01E1E;
    --black:  #0A0A0A;
    --surface:#111111;
    --card:   #141414;
    --border: rgba(212,43,43,0.12);
    --white:  #F5F5F5;
  }

  .section-label {
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.7rem; font-weight:700;
    letter-spacing:0.35em; text-transform:uppercase;
    color:var(--red);
  }
  .display-title { font-family:'Bebas Neue',sans-serif; letter-spacing:0.05em; }
  .body-text     { font-family:'Barlow',sans-serif; }

  .grid-bg {
    background-image:
      linear-gradient(rgba(212,43,43,0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(212,43,43,0.025) 1px, transparent 1px);
    background-size: 52px 52px;
  }
  .noise::after {
    content:''; position:absolute; inset:0;
    background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events:none; z-index:0;
  }

  .reveal {
    opacity:0; transform:translateY(24px);
    transition:opacity 0.65s ease, transform 0.65s ease;
  }
  .reveal.visible { opacity:1; transform:translateY(0); }
  .reveal-delay-1 { transition-delay:0.08s; }
  .reveal-delay-2 { transition-delay:0.16s; }
  .reveal-delay-3 { transition-delay:0.24s; }

  /* ── Ticker ──────────────────────────────────────────── */
  .ticker-wrap { background:var(--red); overflow:hidden; padding:0.55rem 0; }
  .ticker-track { display:flex; width:max-content; animation:ticker 28s linear infinite; }
  .ticker-item {
    font-family:'Bebas Neue',sans-serif; font-size:1rem;
    letter-spacing:0.15em; color:rgba(255,255,255,0.9);
    padding:0 2rem; white-space:nowrap;
  }
  .ticker-item::before { content:'◆'; margin-right:2rem; opacity:0.5; }
  @keyframes ticker { from{transform:translateX(0)} to{transform:translateX(-50%)} }

  /* ── Quick features bar ──────────────────────────────── */
  .features-bar {
    display:flex; flex-wrap:wrap;
    background:var(--card);
    border-bottom:1px solid var(--border);
  }
  .feat-item {
    flex:1; min-width:140px;
    display:flex; align-items:center; gap:0.6rem;
    padding:1.2rem 1.5rem;
    border-right:1px solid var(--border);
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.72rem; font-weight:700;
    letter-spacing:0.18em; text-transform:uppercase;
    color:rgba(255,255,255,0.5);
    transition:color 0.2s, background 0.2s;
  }
  .feat-item:last-child { border-right:none; }
  .feat-item:hover { color:white; background:rgba(212,43,43,0.04); }
  .feat-dot {
    width:6px; height:6px; border-radius:50%;
    background:var(--red); box-shadow:0 0 6px var(--red);
    flex-shrink:0;
  }

  /* ── Facility cards ──────────────────────────────────── */
  .facilities-section {
    position:relative; background:var(--black);
    padding:5.5rem 0 6rem;
  }

  .facility-card {
    position:relative;
    background:var(--card);
    border:1px solid var(--border);
    border-radius:3px; overflow:hidden;
    transition:border-color 0.3s, transform 0.3s, box-shadow 0.3s;
  }
  .facility-card:hover {
    border-color:rgba(212,43,43,0.5);
    transform:translateY(-6px);
    box-shadow:0 20px 60px rgba(0,0,0,0.5);
  }
  /* red top stripe */
  .facility-card::before {
    content:''; position:absolute;
    top:0; left:0; right:0; height:2px;
    background:var(--red);
    transform:scaleX(0); transform-origin:left;
    transition:transform 0.35s ease;
    z-index:3;
  }
  .facility-card:hover::before { transform:scaleX(1); }

  /* media area */
  .facility-media {
    position:relative; height:14rem; overflow:hidden;
    background:linear-gradient(135deg, #1a0000 0%, var(--card) 100%);
  }
  .facility-media img,
  .facility-media video {
    width:100%; height:100%; object-fit:cover;
    transition:transform 0.6s ease, filter 0.4s;
    filter:brightness(0.7);
  }
  .facility-card:hover .facility-media img,
  .facility-card:hover .facility-media video {
    transform:scale(1.06);
    filter:brightness(0.9);
  }
  .facility-media-overlay {
    position:absolute; inset:0;
    background:linear-gradient(to top, rgba(10,10,10,0.92) 0%, rgba(10,10,10,0.1) 55%, transparent 100%);
    z-index:1;
  }
  /* icon placeholder if no image */
  .facility-icon-placeholder {
    width:100%; height:14rem;
    display:flex; align-items:center; justify-content:center;
    background:linear-gradient(135deg, #1a0000 0%, #141414 100%);
    font-size:6rem;
    position:relative; overflow:hidden;
  }
  .facility-icon-placeholder::after {
    content:''; position:absolute; inset:0;
    background:radial-gradient(circle at center, rgba(212,43,43,0.1) 0%, transparent 65%);
  }

  /* type badge */
  .facility-type-badge {
    position:absolute; top:0.8rem; left:0.8rem; z-index:2;
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.6rem; font-weight:700;
    letter-spacing:0.2em; text-transform:uppercase;
    background:rgba(212,43,43,0.9); color:white;
    padding:0.2rem 0.6rem; border-radius:2px;
    backdrop-filter:blur(8px);
  }
  /* availability dot */
  .facility-avail {
    position:absolute; top:0.8rem; right:0.8rem; z-index:2;
    display:flex; align-items:center; gap:0.35rem;
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.6rem; font-weight:700;
    letter-spacing:0.15em; color:rgba(37,211,102,0.9);
    background:rgba(0,0,0,0.6);
    padding:0.2rem 0.55rem; border-radius:2px;
    backdrop-filter:blur(8px);
  }
  .avail-dot {
    width:5px; height:5px; border-radius:50%;
    background:#25d366; box-shadow:0 0 5px #25d366;
    animation:blink 1.8s ease-in-out infinite;
  }
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.3} }

  /* card body */
  .facility-body { padding:1.5rem 1.6rem 1.8rem; }
  .facility-name {
    font-family:'Bebas Neue',sans-serif;
    font-size:1.6rem; letter-spacing:0.05em;
    color:white; line-height:1; margin-bottom:0.3rem;
    transition:color 0.2s;
  }
  .facility-card:hover .facility-name { color:var(--red); }
  .facility-subtitle {
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.7rem; font-weight:700;
    letter-spacing:0.2em; text-transform:uppercase;
    color:rgba(255,255,255,0.3); margin-bottom:1.2rem;
  }

  /* features list */
  .feat-list { display:flex; flex-direction:column; gap:0.3rem; margin-bottom:1.4rem; }
  .feat-row {
    display:flex; align-items:center; gap:0.55rem;
    font-family:'Barlow',sans-serif;
    font-size:0.8rem; color:rgba(255,255,255,0.45);
    padding:0.35rem 0;
    border-bottom:1px solid rgba(255,255,255,0.04);
    transition:color 0.2s;
  }
  .feat-row:last-child { border-bottom:none; }
  .feat-row:hover { color:rgba(255,255,255,0.8); }
  .feat-check {
    width:14px; height:14px;
    border:1px solid rgba(212,43,43,0.4);
    border-radius:2px;
    display:flex; align-items:center; justify-content:center;
    flex-shrink:0;
  }
  .feat-check svg { color:var(--red); }

  /* reserve btn */
  .btn-reserve {
    display:flex; align-items:center; justify-content:center; gap:0.5rem;
    font-family:'Bebas Neue',sans-serif;
    font-size:0.9rem; letter-spacing:0.15em;
    padding:0.8rem;
    background:var(--red); color:white;
    border-radius:2px; text-decoration:none; width:100%;
    position:relative; overflow:hidden;
    transition:background 0.25s, transform 0.2s, box-shadow 0.25s;
  }
  .btn-reserve::before {
    content:''; position:absolute; inset:0;
    background:linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transform:translateX(-100%); transition:transform 0.5s ease;
  }
  .btn-reserve:hover::before { transform:translateX(100%); }
  .btn-reserve:hover {
    background:var(--red-2);
    transform:translateY(-2px);
    box-shadow:0 8px 24px rgba(212,43,43,0.4);
  }

  /* ── Info section ────────────────────────────────────── */
  .info-section {
    position:relative; background:var(--surface);
    padding:5.5rem 0;
  }

  .info-card {
    background:var(--card);
    border:1px solid var(--border);
    border-radius:3px; padding:2rem 2.2rem;
    position:relative; overflow:hidden;
    transition:border-color 0.3s, transform 0.3s;
  }
  .info-card:hover { border-color:rgba(212,43,43,0.4); transform:translateY(-4px); }
  .info-card::before {
    content:''; position:absolute;
    top:0; left:0; right:0; height:2px;
    background:var(--red);
  }
  .info-card h3 {
    font-family:'Bebas Neue',sans-serif;
    font-size:1.4rem; letter-spacing:0.06em;
    color:white; margin-bottom:1.2rem;
    display:flex; align-items:center; gap:0.6rem;
  }
  .info-card h3 .card-icon {
    width:34px; height:34px;
    border:1px solid var(--border);
    border-radius:3px;
    background:rgba(212,43,43,0.06);
    display:flex; align-items:center; justify-content:center;
    font-size:1rem;
  }
  .info-list-item {
    display:flex; align-items:flex-start; gap:0.55rem;
    font-family:'Barlow',sans-serif;
    font-size:0.82rem; color:rgba(255,255,255,0.45);
    padding:0.55rem 0;
    border-bottom:1px solid rgba(255,255,255,0.04);
    line-height:1.5; transition:color 0.2s;
  }
  .info-list-item:last-child { border-bottom:none; }
  .info-list-item:hover { color:rgba(255,255,255,0.8); }
  .info-bullet {
    width:4px; height:4px; border-radius:50%;
    background:var(--red); margin-top:0.55rem;
    flex-shrink:0; opacity:0.7;
  }

  /* hours strip */
  .hours-strip {
    display:grid;
    grid-template-columns:repeat(2,1fr);
    gap:0;
    background:var(--card);
    border:1px solid var(--border);
    border-radius:3px; overflow:hidden;
    margin-top:1.5rem;
  }
  @media(min-width:768px) { .hours-strip { grid-template-columns:repeat(3,1fr); } }
  .hours-cell {
    padding:1.3rem 1.5rem;
    border-right:1px solid var(--border);
    border-bottom:1px solid var(--border);
    transition:background 0.2s;
  }
  .hours-cell:hover { background:rgba(212,43,43,0.04); }
  .hours-day-name {
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.6rem; font-weight:700;
    letter-spacing:0.25em; text-transform:uppercase;
    color:rgba(255,255,255,0.25); margin-bottom:0.2rem;
  }
  .hours-time-val {
    font-family:'Bebas Neue',sans-serif;
    font-size:1.2rem; letter-spacing:0.05em; color:white;
  }

  /* notice block */
  .notice-block {
    background:rgba(212,43,43,0.04);
    border:1px solid rgba(212,43,43,0.15);
    border-left:2px solid var(--red);
    border-radius:2px; padding:1.2rem 1.5rem;
    margin-top:1.5rem;
  }
  .notice-title {
    font-family:'Barlow Condensed',sans-serif;
    font-size:0.65rem; font-weight:700;
    letter-spacing:0.28em; text-transform:uppercase;
    color:var(--red); margin-bottom:0.8rem;
  }
  .notice-grid {
    display:grid; grid-template-columns:1fr;
    gap:0.4rem;
  }
  @media(min-width:640px) { .notice-grid { grid-template-columns:1fr 1fr; } }
  .notice-item {
    font-family:'Barlow',sans-serif;
    font-size:0.78rem; color:rgba(255,255,255,0.4);
    display:flex; align-items:flex-start; gap:0.5rem;
  }
  .notice-item::before { content:'—'; color:rgba(212,43,43,0.5); flex-shrink:0; }

  /* ── Video showcase rediseño ───────────────────────────── */
  .showcase-section {
    position:relative; background:var(--black);
    padding:5.5rem 0; overflow:hidden;
  }

  /* ── Video showcase grid ────────────────────────────── */
  .showcase-media-grid {
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:0.75rem;
  }

  .showcase-img-card {
    position:relative;
    border-radius:6px;
    overflow:hidden;
    border:1px solid var(--border);
    aspect-ratio:4/3;
    background:var(--card);
    transition:border-color 0.3s;
  }
  .showcase-img-card:hover { border-color:rgba(212,43,43,0.4); }
  .showcase-img-card img,
  .showcase-img-card video {
    width:100%; height:100%;
    object-fit:cover;
    filter:brightness(0.75);
    transition:transform 0.5s, filter 0.3s;
  }
  .showcase-img-card:hover img,
  .showcase-img-card:hover video { transform:scale(1.05); filter:brightness(0.9); }
  .showcase-img-card .sc-overlay {
    position:absolute; inset:0;
    background:linear-gradient(to top, rgba(10,10,10,0.7) 0%, transparent 55%);
    z-index:1;
  }
  .showcase-img-card .sc-lbl {
    position:absolute; bottom:0.6rem; left:0.8rem; z-index:2;
    font-family:'Bebas Neue',sans-serif;
    font-size:0.75rem; letter-spacing:0.1em; color:white;
  }

  /* ── CTA ─────────────────────────────────────────────── */
  .cta-section {
    position:relative; background:var(--red);
    padding:5.5rem 0; text-align:center; overflow:hidden;
  }
  .cta-section::before {
    content:'RESERVÁ';
    font-family:'Bebas Neue',sans-serif;
    font-size:clamp(5rem,14vw,11rem);
    color:rgba(0,0,0,0.1);
    position:absolute; top:50%; left:50%;
    transform:translate(-50%,-50%);
    white-space:nowrap; pointer-events:none; letter-spacing:0.06em;
  }
  .cta-grid-bg {
    position:absolute; inset:0;
    background-image:
      linear-gradient(rgba(0,0,0,0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,0,0,0.06) 1px, transparent 1px);
    background-size:48px 48px;
  }
  .btn-cta-white {
    display:inline-flex; align-items:center; gap:0.7rem;
    font-family:'Bebas Neue',sans-serif;
    font-size:1.15rem; letter-spacing:0.15em;
    padding:1rem 2.8rem; background:white; color:var(--red);
    border-radius:2px; text-decoration:none;
    position:relative; overflow:hidden;
    transition:transform 0.2s, box-shadow 0.3s;
  }
  .btn-cta-white::before {
    content:''; position:absolute; inset:0;
    background:linear-gradient(90deg,transparent,rgba(212,43,43,0.06),transparent);
    transform:translateX(-100%); transition:transform 0.5s ease;
  }
  .btn-cta-white:hover::before { transform:translateX(100%); }
  .btn-cta-white:hover { transform:translateY(-3px); box-shadow:0 16px 48px rgba(0,0,0,0.3); }
  .btn-outline-white {
    display:inline-flex; align-items:center; gap:0.6rem;
    font-family:'Bebas Neue',sans-serif;
    font-size:1.1rem; letter-spacing:0.15em;
    padding:0.95rem 2.4rem; background:transparent;
    color:white; border:1.5px solid rgba(255,255,255,0.35);
    border-radius:2px; text-decoration:none;
    transition:border-color 0.25s, background 0.25s, transform 0.2s;
  }
  .btn-outline-white:hover {
    border-color:white; background:rgba(255,255,255,0.08);
    transform:translateY(-3px);
  }
</style>

  <!-- Hero -->
  <Hero variant="page" title="ALQUILER DE CANCHAS" subtitle="Instalaciones profesionales para partidos, eventos y torneos" bgImage="/imagenes/club/canchas.jpg" />

  <!-- Ticker -->
  <div class="ticker-wrap">
    <div class="ticker-track">
      {['Cancha Principal','Fútbol 11','Polideportivo','Fútbol 7','Multiuso Techado','Eventos','Torneos','Reservá ahora',
        'Cancha Principal','Fútbol 11','Polideportivo','Fútbol 7','Multiuso Techado','Eventos','Torneos','Reservá ahora'].map(i => (
        <span class="ticker-item">{i}</span>
      ))}
    </div>
  </div>

  <!-- Quick features bar -->
  <div class="features-bar">
    {[
      { icon:'🚿', label:'Vestuarios con duchas'   },
      { icon:'💡', label:'Iluminación nocturna'     },
      { icon:'🅿️', label:'Estacionamiento'          },
      { icon:'🔒', label:'Seguridad 24hs'           },
      { icon:'📅', label:'Reservas online / WA'     },
    ].map(f => (
      <div class="feat-item">
        <div class="feat-dot"></div>
        <span>{f.icon}</span>
        {f.label}
      </div>
    ))}
  </div>

  <!-- ══════════════════════════════════════════════════════
       FACILITIES CARDS
  ══════════════════════════════════════════════════════ -->
  <section class="facilities-section grid-bg noise">
    <div style="position:absolute;width:45vw;height:45vw;border-radius:50%;background:var(--red);opacity:0.04;filter:blur(90px);top:-5%;left:-10%;pointer-events:none;"></div>
    <div style="position:absolute;width:30vw;height:30vw;border-radius:50%;background:var(--red);opacity:0.03;filter:blur(70px);bottom:-5%;right:-5%;pointer-events:none;"></div>

    <div class="container mx-auto px-4 relative z-10">

      <div class="flex flex-col md:flex-row md:items-end md:justify-between mb-12 gap-4 reveal">
        <div>
          <p class="section-label mb-2">Disponible para alquiler</p>
          <h2 class="display-title text-white" style="font-size:clamp(2.5rem,5vw,4.5rem); line-height:1.02">
            NUESTRAS<br><span style="color:var(--red)">CANCHAS</span>
          </h2>
        </div>
        <p class="body-text" style="font-size:0.82rem; color:rgba(255,255,255,0.3); max-width:260px; text-align:right; line-height:1.6;">
          Consultá disponibilidad de horarios directamente por WhatsApp — respondemos en minutos.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
        {[
          {
            name: 'Cancha Principal',
            type: 'Fútbol 11',
            icon: '⚽',
            video: '/videos/cancha-principal.mp4',
            image: '/imagenes/club/canchas.jpg',
            features: ['Césped natural','Iluminación profesional','Tribunas','Vestuarios amplios','Capacidad: 11 vs 11'],
            wa: 'Hola!%20Quiero%20consultar%20el%20alquiler%20de%20la%20Cancha%20Principal'
          },
          {
            name: 'Cancha N° 2',
            type: 'Fútbol 7',
            icon: '🥅',
            video: '/videos/cancha-2.mp4',
            image: '/imagenes/club/cancahs2.jpg',
            features: ['Césped sintético','Iluminación LED','Vestuarios','Estacionamiento','Capacidad: 7 vs 7'],
            wa: 'Hola!%20Quiero%20consultar%20el%20alquiler%20de%20Cancha%20N%C2%B02'
          },
          {
            name: 'Polideportivo',
            type: 'Multiuso Techado',
            icon: '🏀',
            video: '/videos/polideportivo.mp4',
            image: '/imagenes/club/cancahgim1.webp',
            features: ['Básquet · Handball · Fútbol Sala','Piso reglamentario','Vestuarios','Gradas','Techado'],
            wa: 'Hola!%20Quiero%20consultar%20el%20alquiler%20del%20Polideportivo'
          }
        ].map((f, i) => (
          <article class={`facility-card reveal reveal-delay-${i+1}`}>
            <!-- Media -->
            <div class="facility-media">
              {f.image
                ? <img src={f.image} alt={f.name} loading="lazy" />
                : (
                  <div class="facility-icon-placeholder">
                    <span style="font-size:6rem; position:relative; z-index:2;">{f.icon}</span>
                  </div>
                )
              }
              <div class="facility-media-overlay"></div>
              <div class="facility-type-badge">{f.type}</div>
              <div class="facility-avail"><div class="avail-dot"></div>DISPONIBLE</div>
            </div>

            <!-- Body -->
            <div class="facility-body">
              <div class="facility-name">{f.name}</div>
              <div class="facility-subtitle">{f.type}</div>

              <div class="feat-list">
                {f.features.map(feat => (
                  <div class="feat-row">
                    <div class="feat-check">
                      <svg width="8" height="8" viewBox="0 0 12 12" fill="none">
                        <path d="M2 6l3 3 5-5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </div>
                    {feat}
                  </div>
                ))}
              </div>

              <a
                href={`https://wa.me/542804306350?text=${f.wa}`}
                target="_blank" rel="noopener noreferrer"
                class="btn-reserve"
              >
                <svg width="14" height="14" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
                </svg>
                CONSULTAR DISPONIBILIDAD
              </a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════
       MEDIA SHOWCASE — videos / fotos
  ══════════════════════════════════════════════════════ -->
  <section class="showcase-section">
    <div style="position:absolute;width:40vw;height:40vw;border-radius:50%;background:var(--red);opacity:0.04;filter:blur(80px);bottom:-10%;right:-5%;pointer-events:none;"></div>
    <div class="container mx-auto px-4 relative z-10">

      <div class="reveal reveal-delay-1">

        <!-- Título -->
        <div class="mb-8">
          <p class="section-label mb-2">Mirá nuestras instalaciones</p>
          <h2 class="display-title text-white" style="font-size:clamp(2.5rem,4vw,3.8rem); line-height:1.02">
            LAS CANCHAS<br><span style="color:var(--red)">EN ACCIÓN</span>
          </h2>
        </div>

        <!-- Grid 2×2 de medios -->
        <div class="showcase-media-grid">

          <div class="showcase-img-card">
            <video autoplay muted loop playsinline src="/videos/cancha1.mp4"></video>
            <div class="sc-overlay"></div>
            <div class="sc-lbl">Cancha de Fútbol 7</div>
          </div>

          <div class="showcase-img-card">
            <video src="/videos/elClub.mp4" autoplay muted loop playsinline></video>
            <div class="sc-overlay"></div>
            <div class="sc-lbl">Club Atlético Independiente</div>
          </div>

          <div class="showcase-img-card">
            <img src="/imagenes/club/canchas.jpg" alt="Cancha Principal" loading="lazy" />
            <div class="sc-overlay"></div>
            <div class="sc-lbl">Cancha Principal</div>
          </div>

          <div class="showcase-img-card">
            <img src="/imagenes/club/cancahagim.webp" alt="Polideportivo" loading="lazy" />
            <div class="sc-overlay"></div>
            <div class="sc-lbl">Polideportivo</div>
          </div>

        </div>

      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════
       INFO: incluye · reservas · horarios · reglamento
  ══════════════════════════════════════════════════════ -->
  <section class="info-section grid-bg noise">
    <div class="container mx-auto px-4 relative z-10">

      <div class="mb-12 reveal">
        <p class="section-label mb-2">Todo lo que necesitás saber</p>
        <h2 class="display-title text-white" style="font-size:clamp(2.5rem,5vw,4rem); line-height:1.02">
          INFORMACIÓN<br><span style="color:var(--red)">GENERAL</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">

        <!-- Incluye -->
        <div class="info-card reveal reveal-delay-1">
          <h3>
            <div class="card-icon">✨</div>
            INCLUYE EN EL ALQUILER
          </h3>
          {[
            'Vestuarios con duchas calientes',
            'Iluminación nocturna LED',
            'Estacionamiento en el predio',
            'Personal de seguridad',
            'Atención personalizada',
          ].map(item => (
            <div class="info-list-item">
              <div class="info-bullet"></div>
              {item}
            </div>
          ))}
        </div>

        <!-- Reservas -->
        <div class="info-card reveal reveal-delay-2">
          <h3>
            <div class="card-icon">📅</div>
            CÓMO RESERVAR
          </h3>
          {[
            'Reservas por WhatsApp o en el club',
            'Mínimo 48 hs de anticipación',
            'Seña del 50% para confirmar el turno',
            'Descuentos para torneos y eventos',
            'Beneficios especiales para socios del club',
          ].map(item => (
            <div class="info-list-item">
              <div class="info-bullet"></div>
              {item}
            </div>
          ))}
        </div>

      </div>

      <!-- Horarios disponibles -->
      <div class="reveal reveal-delay-2">
        <p class="section-label mb-4">Disponibilidad horaria</p>
        <div class="hours-strip">
          {[
            { day:'Lunes a Viernes', time:'8:00 — 23:00' },
            { day:'Sábados',         time:'8:00 — 22:00' },
            { day:'Domingos',        time:'9:00 — 18:00' },
          ].map(h => (
            <div class="hours-cell">
              <div class="hours-day-name">{h.day}</div>
              <div class="hours-time-val">{h.time}</div>
            </div>
          ))}
        </div>
      </div>

      <!-- Reglamento -->
      <div class="notice-block reveal reveal-delay-3">
        <div class="notice-title">⚠️ &nbsp; Reglamento del predio</div>
        <div class="notice-grid">
          {[
            'Prohibido el ingreso de bebidas alcohólicas',
            'Respetar horarios de ingreso y egreso',
            'Uso obligatorio de calzado deportivo',
            'Mantener las instalaciones en buen estado',
            'El club no se responsabiliza por objetos olvidados',
            'Capacidad máxima según la cancha reservada',
          ].map(r => (
            <div class="notice-item">{r}</div>
          ))}
        </div>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════
       CTA FINAL
  ══════════════════════════════════════════════════════ -->
  <section class="cta-section">
    <div class="cta-grid-bg"></div>
    <div class="container mx-auto px-4 relative z-10">
      <p class="section-label mb-3" style="color:rgba(255,255,255,0.6);">Disponibilidad inmediata</p>
      <h2 class="display-title text-white mb-4" style="font-size:clamp(2.8rem,7vw,6rem); line-height:1.02">
        RESERVÁ TU<br>CANCHA HOY
      </h2>
      <p class="body-text mb-10" style="font-size:1rem; color:rgba(255,255,255,0.75); max-width:400px; margin:0 auto 2.5rem; line-height:1.6;">
        Escribinos por WhatsApp y te confirmamos disponibilidad, tarifas y horarios en minutos.
      </p>
      <div style="display:flex; flex-wrap:wrap; gap:1rem; justify-content:center;">
        <a
          href="https://wa.me/542804306350?text=Hola!%20Quiero%20consultar%20sobre%20el%20alquiler%20de%20canchas"
          target="_blank" rel="noopener noreferrer"
          class="btn-cta-white"
        >
          <svg width="20" height="20" fill="currentColor" viewBox="0 0 24 24">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
          </svg>
          RESERVAR POR WHATSAPP
        </a>
        <a href="/deportes" class="btn-outline-white">VER INSTALACIONES</a>
      </div>
    </div>
  </section>

</Layout>

<script>
  const obs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target); }
    });
  }, { threshold: 0.08 });
  document.querySelectorAll('.reveal').forEach(el => obs.observe(el));
</script>